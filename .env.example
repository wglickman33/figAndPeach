# Copy to .env for local development (never commit .env)

# Admin login (planned — used by Netlify Functions + admin page)
# ADMIN_USERNAME=
# ADMIN_PASSWORD=

# Where order submissions are stored/sent (planned — e.g. email, database, Google Sheet)
# ORDER_NOTIFICATION_EMAIL=
